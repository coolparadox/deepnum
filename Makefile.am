SUBDIRS = src
dist_doc_DATA = README

ACLOCAL_AMFLAGS = -I m4

totalclean: maintainer-clean
	-rm -f aclocal.m4 ar-lib compile config.guess config.sub configure depcomp install-sh ltmain.sh
	-rm -f missing Makefile.in src/Makefile.in
	-rm -rf m4/
